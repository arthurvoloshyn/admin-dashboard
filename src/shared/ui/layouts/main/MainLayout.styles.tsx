import * as A from 'antd';
import { styled } from 'styled-components';

export const Layout = styled(A.Layout)`
  height: 100vh;
`;

export const Content = styled(A.Layout.Content)`
  padding: 20px;
`;
